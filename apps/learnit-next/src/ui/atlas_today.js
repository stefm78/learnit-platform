'use strict';
const REASON_LABELS=Object.freeze({NEW_OBJECTIVE:'Nouvel objectif à découvrir.',PRACTICE_IN_PROGRESS:'Entraînement en cours.',RECENT_ERROR:'Une erreur récente doit être reprise.',REVIEW_REQUIRED:'Une correction ciblée est recommandée.',CORRECTION_COMPLETED:'La correction a été effectuée.',NO_INDEPENDENT_VALIDATION:'Aucune validation indépendante admissible.',VALIDATION_AVAILABLE:'Une validation autonome est disponible.',RECENTLY_VALIDATED:'Une validation récente est enregistrée.',SESSION_TIME_LIMIT:'Cette activité ne tient pas dans la durée choisie.'});
function esc(x){return String(x).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));}
function validateRecommendation(rec){if(!rec||rec.recommendationVersion!=='atlas.recommendation.v1')throw new Error('INVALID_RECOMMENDATION');if(!Array.isArray(rec.reasonCodes)||!rec.reasonCodes.length||rec.reasonCodes.some(x=>!REASON_LABELS[x]))throw new Error('UNKNOWN_REASON_CODE');if(!rec.preferredActivityRef||!Number.isInteger(rec.estimatedMinutes))throw new Error('INVALID_RECOMMENDATION');return rec;}
function validatePlan(plan){if(!plan||!plan.payload||plan.payload.schemaVersion!=='atlas.session-plan.v1'||!Array.isArray(plan.payload.items))throw new Error('INVALID_SESSION_PLAN');if(plan.payload.items.some((x,i)=>x.position!==i))throw new Error('INVALID_SESSION_PLAN');return plan;}
function renderToday({recommendation,plan,resumeState}){if(!recommendation||!plan)return '<section class="atlas-m1 atlas-today" aria-labelledby="atlas-today-title"><h1 id="atlas-today-title">Aujourd’hui</h1><p>Aucune séance admissible pour le moment.</p></section>';validateRecommendation(recommendation);validatePlan(plan);const reasons=recommendation.reasonCodes.map(x=>`<li>${esc(REASON_LABELS[x])}</li>`).join('');const items=plan.payload.items.map(x=>`<li><span>${esc(x.action)}</span><strong>${x.estimatedMinutes} min</strong></li>`).join('');const resume=resumeState?'<button type="button" data-atlas-action="resume">Reprendre la séance</button>':'';return `<section class="atlas-m1 atlas-today" aria-labelledby="atlas-today-title"><h1 id="atlas-today-title">Aujourd’hui</h1><p class="atlas-duration">${plan.payload.durationMinutes} minutes · ${plan.payload.totalEstimatedMinutes} prévues</p><ul class="atlas-reasons">${reasons}</ul><ol class="atlas-plan-preview">${items}</ol><div class="atlas-actions">${resume}<button class="atlas-primary" type="button" data-atlas-action="start">Commencer</button></div></section>`;}
module.exports=Object.freeze({REASON_LABELS,validateRecommendation,validatePlan,renderToday});
