/RESEARCH /AUDIT /SOLVE /BUILD

MISSION — E1B / PUBLISH, QUALIFY, AND CLOSE THE EXPERIMENTAL INTEGRATION HANDOFF
Repository: `stefm78/learnit-platform`

You are a fresh governed worker closing the already-authored E1B long-source scale/reuse experiment. Your task is not to redesign E1B, not to promote Knowledge, and not to merge anything. Your task is to publish the frozen experimental payload on its isolated stacked branch, obtain exact-head repository qualification, repair only mechanical defects inside the authorized surface, and produce a truthful final handoff to `/solve`.

## 0. Independence and evidence boundary

Do not use hidden reasoning or remembered conclusions as evidence. Reconstruct authority from the repository and the exact artifacts named here.

Canonical control requirements:
- obey the user-installed constitutional bootstrap;
- perform ACTIVE_KERNEL_REBIND before governed work;
- revalidate project/branch CAS before every mutation;
- correlation IDs are diagnostic only.

Product repository authority:
- repository: `stefm78/learnit-platform`;
- authoritative product branch: `main`;
- observed main at preparation: `1e71271436fd4b7dd7d895cfec827833187630fe` — NON-SOVEREIGN until re-read;
- upstream experimental PR: `#378`;
- upstream branch: `experiment/e1a-r1-coverage-preservation-mi2-complexes`;
- observed upstream head: `a760ce1f8bb1098f122a66beb73ce5d8c6e257b5` — NON-SOVEREIGN until re-read;
- E1B branch: `experiment/e1b-energy-climate-scale-reuse`;
- work package: `EXP-WP-002`.

The upstream reservation must remain explicit:
`UPSTREAM_R1_RESERVATION = INDEPENDENCE_DEGRADED`.
Do not reinterpret PR #378 as merged or production authority.

## 1. Frozen source and payload

External source identity, already byte-verified during E1B authoring:
- file: `EPF_Cours Energie et enjeux climatiques.pdf`;
- SHA-256: `cc411ddb76a904a684a44ce85be15b67a9ef9f2bc75288697a4981a62a4b0c73`;
- bytes: `3042017`;
- pages: `163`;
- source must never be committed.

For this publication/qualification job, do not reopen source semantics unless a candidate must change. If exact source bytes are locally available, recheck their identity before any semantic repair. If they are unavailable, mechanical publication/qualification may continue only if the frozen payload remains byte-identical and no semantic artifact is changed. Any semantic repair without exact source bytes is forbidden.

Frozen experimental root:
`experiments/knowledge-e1b-energy-climate/`

Expected payload properties:
- 8 `learnit.kit.v2` candidates: DIRECT and KNOWLEDGE for K1–K4;
- source manifest/map, scopes, coverage ledgers/checks, Knowledge bundle/relations, blueprints;
- factory contexts and explicitly non-independent comparative semantic-review evidence;
- reuse, complexity, break-even and change-impact evidence;
- K4 narrow-scope and Direct fast-path audits;
- deterministic verifier and CI workflow;
- final report;
- this prompt and the complete execution job.

Do not convert the comparative semantic review into an independent Factory certification. Preserve:
`SEMANTIC_REVIEW_INDEPENDENCE = DEGRADED`.

## 2. Preconditions — fail closed

Before mutation, establish all of the following:
1. current `main` identity;
2. PR #378 is still open, DRAFT and unmerged;
3. current upstream branch head;
4. E1B branch exists and is still based on, or can be safely fast-forwarded from, the expected upstream parent without overwriting independent work;
5. no unrelated E1B PR/branch mutation has appeared;
6. `EXP-WP-002` authorizes only:
   - `work-packages/EXP-WP-002.json`;
   - `.github/workflows/e1b-energy-climate-scale-reuse.yml`;
   - `experiments/knowledge-e1b-energy-climate/**`;
7. no source PDF is present in the commit payload;
8. all JSON is parseable and frozen candidate hashes match the freeze manifest.

On stale or conflicting branch state: `STALE_E1B_BRANCH -> NO MUTATION -> RECONSTRUCT`.
On source-required semantic repair without exact source bytes: `SOURCE_BYTES_REQUIRED`.

## 3. Publication transaction

Publish the complete frozen E1B payload to `experiment/e1b-energy-climate-scale-reuse` using the smallest safe transaction.

Required characteristics:
- one coherent commit is preferred;
- parent must be the current safe E1B branch head established by CAS;
- changed paths must remain entirely inside `EXP-WP-002` scope;
- do not modify `apps/**`, `authoring/**`, `contracts/**`, `docs/**`, `governance/**`, `tools/**`, `tests/**`;
- do not merge or modify PR #378;
- do not alter product runtime/schema/validators/Factory Gate/author-review skills.

Commit intent:
`experiment(e1b): add long-source scale and reuse qualification corpus`

## 4. Draft stacked PR

Open exactly one DRAFT PR:
- head: `experiment/e1b-energy-climate-scale-reuse`;
- base: `experiment/e1a-r1-coverage-preservation-mi2-complexes`;
- never target `main` directly for this experiment;
- never mark ready-for-review automatically;
- never merge automatically.

PR body must identify exactly one governing work package: `EXP-WP-002`.
It must state:
- source identity and that the PDF is not committed;
- K1–K4 / DIRECT-vs-KNOWLEDGE purpose;
- current deterministic claims;
- `200_300_PAGE_SCALE_NOT_DIRECTLY_TESTED`;
- `UPSTREAM_R1_RESERVATION = INDEPENDENCE_DEGRADED`;
- `SEMANTIC_REVIEW_INDEPENDENCE = DEGRADED`;
- no product integration or promotion is authorized.

## 5. Exact-head qualification

After PR creation, qualify the exact PR head.

Required mechanical gates:
1. E1B workflow runs on the PR head;
2. `verify_e1b.py` passes against current canonical Learn-it authorities;
3. every frozen candidate passes:
   - canonical `learnit.kit.v2` validation;
   - Atlas validation;
   - M3.1 `EXCELLENT_BY_PROFILE`;
4. factory-context candidate/brief/source-set/context digests bind correctly;
5. coverage has zero `MISSING_SILENTLY` for K1–K4 Knowledge routes;
6. K4 narrow-scope sentinels pass;
7. change-impact expected/observed sets remain exact;
8. PR delta is entirely within the work-package scope;
9. source PDF is absent from changed files;
10. PR remains DRAFT and unmerged.

If CI fails, inspect the failing job/log. Repair only when:
- the failure is mechanical/repository-shape/qualification evidence;
- the fix is inside `EXP-WP-002` scope;
- the fix does not change the frozen strategic hypothesis or require semantic invention.

After each repair:
- re-CAS the branch;
- update affected frozen hashes/evidence honestly;
- rerun qualification on the new exact head.

If a failure requires candidate semantic changes and exact source bytes are unavailable, HOLD with `SOURCE_BYTES_REQUIRED`.
If a failure implies a product-authority/schema/runtime change, HOLD and return to `/solve`.

## 6. Final adversarial audit

Before final verdict, independently challenge false PASS:
- compare PR base/head changed paths;
- confirm one work package only;
- verify PR DRAFT/unmerged;
- confirm PR #378 unchanged/unmerged;
- confirm main unchanged by this job;
- verify no PDF/binary source leaked;
- verify the final head is the head actually qualified by CI;
- verify report language does not claim independent semantic certification;
- verify the 163-page source is not described as a 200–300-page direct scale test;
- verify Knowledge remains optional and Direct fast path preserved.

## 7. Terminal outcomes

Return exactly one principal state:

`PASS_E1B_INTEGRATION_QUALIFICATION_READY_FOR_SOLVE`
only if the exact final PR head has passed all required mechanical CI/audit gates and the reservations are explicit.

`HOLD_E1B_INTEGRATION_QUALIFICATION`
when publication exists but one or more required gates remain unresolved.

`FAIL_E1B_INTEGRATION_QUALIFICATION`
for a demonstrated invalid experiment result or unrecoverable governance/identity contradiction.

A PASS authorizes only:
`REENTER_SOLVE_FROM_MAIN_FOR_MINIMUM_INTEGRATION`.

A PASS does NOT authorize:
- merging the E1B PR;
- merging PR #378;
- product/runtime/schema integration;
- Knowledge becoming mandatory;
- promotion of any experimental contract;
- claiming independent semantic Factory certification.

## 8. Required final report

Report compactly:
- exact main SHA;
- exact upstream PR #378 head/state;
- exact E1B PR number/base/head/state;
- exact qualified head SHA;
- CI workflow/run/job result;
- changed-file count and scope result;
- candidate qualification summary;
- source identity and non-commit status;
- preserved reservations;
- final principal state;
- safe next action.
