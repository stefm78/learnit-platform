{
  "schemaVersion": 1,
  "id": "GOV-WP-015",
  "title": "Make Remote Agent transport compatible with canonical PR scope enforcement",
  "status": "accepted",
  "changeClass": "repository-governance-tooling",
  "riskClass": "high",
  "purpose": "Remove the structural contradiction in which Remote Agent execution requires .agent-jobs/**/READY while PR scope rejects every .agent-jobs/** path. Permit only a fully validated three-file Remote Agent transport envelope and enforce the canonical work-package scope against the intended patch and tested result paths.",
  "baseline": {
    "sourceRepository": "stefm78/learnit-platform",
    "baseCommit": "8c4c91c2b84c9fd2dea02a9a07cd249568bcffbf",
    "candidateName": "remote-agent-pr-scope-compatible-gate"
  },
  "scope": {
    "allowedPaths": [
      "work-packages/GOV-WP-015.json",
      "tools/validate_pr_scope.py",
      "tools/test_validate_pr_scope.py",
      "docs/governance/PR_SCOPE_GATE.md",
      ".github/workflows/pr-scope.yml"
    ],
    "forbiddenPaths": [
      "apps/player/**",
      "contracts/**",
      "platform/**",
      "governance/**",
      "docs/architecture/**",
      "tools/agent_worktree.py",
      "tools/validate_repository.py",
      ".github/workflows/player-ci.yml",
      ".github/workflows/remote-agent-worktree.yml"
    ]
  },
  "invariants": [
    "Ordinary pull requests continue to reject all generated and transport paths.",
    "A Remote Agent pull request is recognized only when one job directory contains exactly READY, change.patch and job.json.",
    "The job baseCommit must equal the pull-request base SHA and the job branch must use agent/**.",
    "Remote Agent allowedPaths must be unique exact file paths without globs and each must be permitted by the canonical work package named in the pull-request body.",
    "The patch must be applicable text, contain unique paths, and every intended path must satisfy both the job and canonical work-package scopes.",
    "After the tested-result commit, every non-transport changed path must remain inside both the exact job allowedPaths and canonical work-package scope.",
    "The PR-scope workflow changes only by adding the focused validator regression command before live scope validation.",
    "No Remote Agent runner, player source, contract, architecture or platform behavior changes under this work package.",
    "The first reversible seam and Stage D remain blocked until this gate repair is merged and the seam is rerun from the new exact main."
  ],
  "requiredEvidence": [
    "focused regression test covering a valid envelope and tested result",
    "negative tests for missing transport files, globbed job paths and canonical-scope escape",
    "Repository governance success",
    "PR scope success on this ordinary governance pull request",
    "subsequent Remote Agent envelope and tested-result PR scope success before acceptance"
  ],
  "adversarialTests": [
    "reject an envelope missing READY, change.patch or job.json",
    "reject more than one Remote Agent job directory or any extra transport file",
    "reject a job baseCommit different from the pull-request base SHA",
    "reject globbed, duplicate, generated or canonically forbidden job allowedPaths",
    "reject a patch path or tested-result path outside either the exact job scope or canonical work-package scope",
    "reject binary patches, archives, symbolic links and submodules"
  ],
  "nonGoals": [
    "exempt agent branches from canonical scope",
    "allow arbitrary .agent-jobs contents",
    "weaken generated-file or archive prohibitions",
    "change Remote Agent implementation semantics",
    "change player code or begin Stage D"
  ],
  "independentReview": [
    "Repository governance validates the complete work-package contract",
    "PR scope validates this ordinary governance pull request against GOV-WP-015",
    "focused tests execute inside the exact PR-scope workflow before live validation",
    "the next clean Remote Agent envelope and tested-result commits must each pass PR scope independently"
  ],
  "result": {
    "decision": "GO_WITH_CONDITIONS",
    "implementationAllowed": true,
    "implementationAccepted": false,
    "acceptanceGate": "focused tests plus successful first-seam envelope and tested-result scope evidence"
  },
  "rollback": "Revert the validator, focused test and one workflow test step. Remote Agent PRs become blocked again by PR scope; no player or learner data rollback is required."
}
